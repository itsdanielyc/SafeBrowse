# Verification record

## First public preview preparation — 5 September 2026

The public project is [itsdanielyc/SafeBrowse](https://github.com/itsdanielyc/SafeBrowse). The README targets supported recording and screen-sharing workflows, includes the owner's unchanged product overview and original demo assets, and ends with collapsed troubleshooting entries. Its playable attachment is a smaller H.264/AAC derivative of the same supplied animation. Private vulnerability reporting is enabled; no contact mailbox is implied by a GitHub commit noreply address.

A monitor-movement defect was corrected before this release: browser clamping now chooses the monitor from the proposed title bar instead of the old window position. Seven new regressions cover upward/downward and negative-origin transitions, programmatic/no-size movement, off-display reachability and minimized/no-move preservation. This is not a physical mixed-DPI or display-disconnection test. Normal input-desktop switching still applies across displays, and Windows still chooses the initial window placement.

Local release checks passed: **164 Rust tests**, **4 ignored**; **21 Node tests**; formatting; all-target Clippy with warnings denied; **11 isolated installer scenarios**; and the release-tooling fixture. The RustSec Windows filter reported **0 applicable findings**, retaining **12 other-platform findings** in its report. The separate attribution inventory verifies **123 normal/build dependency packages**, with pinned upstream supplements for omitted WebView2 crate/SDK license text. Neither check certifies the application or the separately installed runtime as uncompromised.

The installer now includes repository/support/update links and third-party notices. Public packaging uses `New-Installer.ps1 -RequireClean` after committing the final source, and release assets include the matching checksum/provenance. The initial installer is a local build from that committed source, not a GitHub-attested artifact. CI and chart workflow outcomes can be inspected on the repository's [Actions page](https://github.com/itsdanielyc/SafeBrowse/actions).

The star graph records real daily UTC totals on a separate data branch, including decreases; it does not fabricate pre-publication history or forward a personal token to an external chart service. Seven tests cover its validation, initial zero state, no-op updates and non-forced publication behavior.

Capture exclusion with OBS and Windows recording is maintainer-reported, with no exact recorder versions or modes supplied. Actual WebView2 installation on a fresh machine, Google Meet capture paths, physical multi-monitor/mixed-DPI transitions and monitor hot-plug remain platform checks. The original `motion` and `showcase-product` projects were left intact; only the explicitly supplied final media were copied into tracked documentation assets.

## Unsigned installer compilation and integration — 5 September 2026

After the user installed Inno Setup, the official **7.1.0** compiler was found at `%LOCALAPPDATA%\Programs\Inno Setup 7\ISCC.exe`; its Authenticode signature validates as **Pyrsys B.V.**. The earlier automatic-review block is resolved by that user-performed installation.

The first real build exposed two build-script assumptions, both corrected: Inno's executable PE version fields are `0.0.0.0`, so the verified compiler's `--version` command identifies its engine; PowerShell's direct GUI invocation did not reliably capture help output, so payload checks now use explicit subprocess output pipes. Compiler options explicitly disable inherited IDE signing tools and signing, as requested.

`scripts/New-Installer.ps1` successfully rebuilt the existing payload and compiled the **unsigned** `SafeBrowse-Setup-0.1.0-x64.exe`. SHA-256 and `NotSigned` status were checked against the generated provenance. Each build has its own `target/installer/<build-id>` folder with the installer, `SHA256SUMS.txt`, and `provenance.json`. The application/maintenance binary hashes and six read-only smoke results remain those recorded below; no Rust application code changed in this compilation follow-up.

`scripts/Test-Installer.ps1` passed **11 native integration scenarios**, using production-derived scripts and standalone Rust mock executables fixed to fresh disposable roots. Scenarios cover blocked runtime configuration without bootstrap execution, missing runtime with bootstrap failure, initial install, same-version reinstall, downgrade refusal, custom-destination refusal, cleanup failure preserving the application/uninstaller, silent removal preserving data, the prompt's default No, Cancel before cleanup, and explicit data-removal arguments. Creation and removal of the unique Start menu shortcut and Windows uninstall entry were verified. Inno's second phase needs a bounded wait for self-deletion; the test now waits up to ten seconds before asserting uninstaller-file removal.

Evidence is `target/installer-tests/6e6b0b517c6f4eaa8ae1cae3e8aa3c66/results.json` and its adjacent logs. Its source digest matches `installer/SafeBrowse.iss`: `b51b048971801010b7ea0648f86295c2fb15f6e2cfa308bcc0ecc43d66631446`. The test fixtures did not execute SafeBrowse, the real maintenance helper, or Microsoft's runtime installer. Prompt choices were injected into fixture source; actual visual interaction and runtime installation on a machine without WebView2 are still manual checks. User browsing data, the shared runtime, the approved main executable and `motion` were untouched. Nothing was published to GitHub.

## Unsigned small installer preparation — 5 September 2026

The [installer specification and build instructions](INSTALLER.md) describe the per-user setup, runtime handling and uninstall choices. The application now uses the GUI subsystem in release builds, preserves visible GUI errors and redirected diagnostics, includes Windows version/publisher resources, and rejects new sessions during installer maintenance. A separate maintenance binary provides typed runtime checks and bounded, allowlisted cleanup under the session mutex.

- `cargo test --locked --offline`: **157 passed, 4 ignored, 0 failed**. New coverage includes GUI/redirected launch classification, fresh-named maintenance mutexes, runtime setup authorization, per-user cleanup opt-in, paths that escape through reparse points, hard links, locks, preserved downloads/unknown files, empty temporary-root removal and ownership-marker restoration after failed deletion.
- `cargo fmt --check` and `cargo clippy --all-targets --locked --offline -- -D warnings` passed.
- `cargo build --release --locked --offline --bins --target-dir target/installer-app` passed. The application PE subsystem is **2 (Windows GUI)**, product version **0.1.0**, publisher metadata **SafeBrowse Contributors**.
- Six subprocess smoke cases passed with captured output: app help, invalid app argument, unauthenticated worker, installed runtime check, a child-only forbidden runtime override, and rejected arbitrary maintenance cleanup arguments. No smoke case started a browsing session or deleted actual user data. WebView2 discovery returned **152.0.4191.62**.
- Application SHA-256: `c64b4bf25a72eaca3215b8e3d85160848972be617ea3923e221b875f6f6c586f`; maintenance SHA-256: `7de15b3fbbccb31d351923e6f07d6291ba457f784c2514cf45b0d24c75b9037c`. Both are in `target/installer-app/release`. The previously approved `target/release/safebrowse.exe` was not replaced.
- `scripts/New-Installer.ps1` passed PowerShell syntax parsing. `installer/SafeBrowse.iss` was reviewed against Inno Setup 7.1.0's official source for API/type availability, brace expansion and uninstall callback order. **Actual Inno compilation and end-to-end installer tests have not run. No SafeBrowse setup EXE has been produced.**

The official Inno Setup 7.1.0 x64 download is cached at `target/installer-tools/innosetup-7.1.0-x64.exe`, SHA-256 `0362a383ed217d4c4239b5933866dd96d3eb2102737da92f80f6057a4b40df2f`. Its digest matches the official GitHub release asset and its Authenticode signature validates as **Pyrsys B.V.**. Automatic approval review rejected running its portable setup with **“blocked by policy”**, without a more specific reason. No alternative launch path was attempted. No existing Inno compiler was found.

Microsoft's small Evergreen Bootstrapper is cached at `target/installer-tools/MicrosoftEdgeWebview2Setup.exe`, SHA-256 `17debf797a6c737959bc588236e897936ffac1af5f7e515e674ab32f9edfe719`. Its Authenticode signature validates as **Microsoft Corporation**. It was downloaded and inspected but not executed. No shared runtime or Windows security setting was changed. The other agent's `motion` directory was not read or modified. No GitHub repository, release, signature or paid account was created.

## Normal-close cleanup correction — 4 September 2026

The [shutdown correction record](reviews/shutdown-cleanup-fix-2026-09-04.md) documents the user's error, the deterministic reproduction, and the exact scope. A cache child disappearing after directory enumeration no longer aborts cleanup; all remaining deletion and ownership checks are still enforced. Native error dialogs now use **SafeBrowse error** so shutdown failures are not labelled as startup failures.

`cargo test --locked --offline` passed **137 tests**, with **4 ignored**. This includes nine profile regressions and a new three-session hidden production WebView2 shutdown fixture checking profile removal, normal process exit, no false failure notification, and no repeated page request. Formatting and all-target Clippy with warnings denied passed. The full visible isolated-desktop application was not relaunched; the user can repeat their ordinary open/close check with the rebuilt executable.

`cargo build --release --locked --offline --target-dir target/security-remediation` and the resulting executable's `--help` check passed. The updated `target/security-remediation/release/safebrowse.exe` has SHA-256 `a19ca271e00caf70e40efd93fa1be11185aa19607e0cae13f3aa34565212c8a1`. This replaces the earlier local binary at that path. The [new source manifest](reviews/shutdown-cleanup-fix-2026-09-04.sha256) supersedes the earlier manifest for identifying the current build inputs; previous records below remain historical.

## Security review remediation — 4 September 2026

The [remediation record](reviews/security-remediation-2026-09-04.md) describes the exact scope and remaining boundaries. Fresh verification after these changes:

- `cargo test --locked --offline`: **134 passed, 4 ignored, 0 failed**. This includes real production website/popup-builder print-wrapper tests and termination of only a hidden, disposable WebView2 browser process to verify native failure notification without a repeated page request.
- The otherwise ignored `browser::runtime::tests::installed_runtime_is_discoverable_without_creating_a_session` was run explicitly and passed. Its read-only preflight selected WebView2 **152.0.4191.62**. The earlier recorded **151.0.4129.107** remains the tested minimum support floor; the comparison accepts newer stable versions.
- Seven new native profile tests passed: active and abandoned leases; missing, copied, oversized and malformed markers; concurrent root creation; cleanup failure/retry; bounded/unowned scans; junctions/reparse points; and hardlinks. Tests use injected temporary roots and dummy files. The existing delayed-file-release and locked-profile integration tests also passed.
- `cargo clippy --all-targets --locked --offline -- -D warnings` and `cargo fmt --check` passed. Clippy's initial redundant boolean-conversion finding was corrected. The first full test run exposed missing standard write flags on marker creation; that defect was fixed before the successful full run. Only the verified empty root created by that failed fixture was removed nonrecursively.
- `node --test tests/keyboard_dom_tests.cjs tests/website_print_guard_tests.cjs`: **14 passed**.
- `scripts/Test-ReleaseTooling.ps1` passed clean/dirty/untracked/ignored-source guards, Windows advisory filtering, audit-tool failure handling, isolated Git export, mocked build, packaging, provenance and ZIP checksum checks. The fixture's fake executable is not a product build. The actual dirty checkout is rejected for candidate packaging.
- `scripts/Test-WindowsDependencies.ps1` passed using cargo-audit **0.22.2** and RustSec database revision `5a0ebedfe8bdd2e295b171f4162f8c977bcad9a5` (2 September 2026): **0 Windows-graph findings**, **12 other-platform findings retained**. The resolved Windows graph has 127 packages including SafeBrowse (126 third-party packages). The report is `target/windows-dependency-audit.json`. This does not scan WebView2 binaries or certify dependencies as uncompromised.
- `cargo build --release --locked --offline --target-dir target/security-remediation` passed from this working tree; `target/security-remediation/release/safebrowse.exe --help` passed without opening a session. The new executable's SHA-256 is `b506f2e9cbad944e037e2828b8be7955a48640b32e10780dac801803136c9d5a`. Existing executable paths were not overwritten. This is a local review build, not a committed-revision release candidate or a signed/attested public release.

No active SafeBrowse session was interrupted, desktop switched, clipboard cleared, native print dialog invoked, public workflow dispatched, or release published for this follow-up. The new crash test terminates only its positively identified disposable browser process. Full isolated-desktop recovery, renderer-hang injection, supervisor-failure recovery, update-event delivery and protected native-dialog capture still require dedicated platform testing. Printing's previously documented opener-retained native-reference gap remains open.

## Earlier verification

Verified on 3–4 September 2026 with the installed Windows toolchain:

- `cargo fmt --check` and `cargo clippy --all-targets -- -D warnings` pass.
- Earlier full `cargo test --locked` run after website-print suppression: 118 passed; 4 ignored entries (2 explicit interactive/destructive tests, 1 subprocess fixture invoked by its parent test, and 1 installed-runtime fixture run separately).
- `node --test tests/keyboard_dom_tests.cjs tests/website_print_guard_tests.cjs`: 14 behavior tests passed.
- The security candidate was built with `cargo build --release --locked --target-dir target/security-fix`. The subsequent ENG overlay fix was built with `cargo build --release --locked` at `target/release/safebrowse.exe`. The running `target/security-fix` session was not overwritten or terminated. Close the existing session before launching the new executable; second launches refuse instead of attaching to an unverified session.
- `cargo run --example worker_authorization_probe`: passed direct-worker and replay rejection, authenticated same-image child startup, early-exit and timeout rejection, hidden WebView2 rendering, and worker/runtime job-close termination on an unused desktop that never received input.
- The bundled UI preview was inspected in a browser: red recording warning, bookmarks, add-bookmark dialog, settings, compact layout, keyboard toggle, Shift input and automatic Shift reset.
- New permission controls were also captured from hidden WebView2 fixtures and visually inspected: the request prompt at 460 × 350 (normal and long addresses) and Settings at 800 × 600. `cargo run --example ui_preview -- --capture-permissions` regenerates these sample-only images in `target/ui-preview`.

The blank-window regression was reproduced in the native application and in a Windows WebView2 test. The installed runtime reports `NavigateToString` navigation as a base64 HTML data URL before committing `about:blank`; the former filter rejected that bundled document. The shared policy now permits only `about:blank` and the exact HTML document supplied by native code. It applies to the browser controls, recording warning, internal pages, and desktop companion.

Native tests now verify the warning's rendered DOM, CSS, OK-button IPC, replacement Settings document, companion rendering and IPC, and child-view geometry after resizing and sibling destruction. Arbitrary HTML data URLs remain rejected. Browser views use Wry's child-view API so they no longer overwrite each other's parent resize handler. A delayed-file-release test covers asynchronous WebView2 profile cleanup.

The desktop shortcut has one supervisor owner. A native registration probe confirmed that registering the same chord on two Win32 desktops causes error 1409. Regression tests distinguish explicit taskbar restoration and hotkey toggles from passive focus messages, and verify that unavailable desktop handles fail instead of assuming which desktop is active. The former focus-based return and debounce behavior were removed.

Native WebView2 tests verify the taskbar language button, installed layout labels, selection IPC, localized key output, Shift punctuation, separate Caps Lock mappings, reset behavior, and IME guidance. Native window geometry tests verify monitor-bottom taskbar positioning after resizing and moving the browser. Input-language tests cover invalid handles, uninstalled identifiers, regional labels, and renderer-thread selection after focus moves to a shell control.

The picker regression now opens and closes its native window three times while processing queued child-focus notifications. It checks foreground ownership, `document.hasFocus()`, and selected-radio focus on every open. Calling-thread `GetFocus()` cannot identify focus inside WebView2's renderer process; dismissal now uses foreground ownership instead.

Permission-store tests cover exact-origin scheme/host/port and capability isolation, explicit Ask overrides, persistence, revocation, rejected schemas, and failed writes preserving both disk and memory. Native UI tests verify Settings commands, the reload action remaining after Reset, all four prompt decisions, safe focus when the request changes, and scrollable long addresses with all decision buttons visible.

Native request tests use a disposable loopback site and profile. They verify HTTP and `about:blank` popup opener relationships, `postMessage`, `window.close`, initiating-origin attribution for an embedded cross-origin popup, explicit geolocation denial without an engine-persisted decision, a repeat request, and navigation/closure invalidating stale grants. An existing native permission denial is reset before navigation so older profile decisions cannot bypass SafeBrowse's store. Cross-origin device permission rejection is covered at the origin-policy boundary; the hidden WebView fixture does not reliably produce that iframe's native permission event or callback. Camera/microphone access and OS permission prompts were not granted during testing.

Keyboard tests verify that delayed field and rich-text edits preserve the next control's focus. Rich-text coverage includes insertion, backspace across Unicode markup, line breaks, and canceled `beforeinput`. These behavior checks replace the former assertion about an exact `execCommand` source string. Unfocused rich-text edits use a DOM range because Chromium's editing command can reactivate the editor; native undo history is unavailable for that fallback.

The computer-use agent previously attempted to inspect the running isolated-desktop session. Windows returned `GetCursorPos failed: Access is denied (0x80070005)` after the supported activation recovery, so no live clicks or full end-to-end desktop-switch claims are recorded. On the subsequent inspection SafeBrowse was no longer running. The native tests above run on the available desktop without switching the user's desktop.

Input-indicator suppression now scopes its show hook and enumeration to the dedicated desktop, including other owner threads and helper processes. It recognizes the exact documented TSF title and the exact `TrayInputIndicatorWClass` compatibility class. It does not match general IME/CoreWindow classes or visible labels such as EN/ENG. Visibility changes are asynchronous; surviving indicators it hid are restored without activation on guard teardown. No global language preference is changed.

Native regression fixtures use unique desktops that never receive input. They exercise initial, late, and repeated indicator shows from another thread and another process, restoration on teardown, preservation of never-shown windows and ordinary picker/candidate/EN/ENG windows, longer titles with the same prefix, and a separate desktop that must remain untouched. A paused helper thread proves that installation/refresh complete before its message pump resumes; queued visibility changes then complete after it resumes. The ordinary-desktop constructor and invalid HWND are also checked. The subprocess fixture is ignored as a standalone test and is invoked by its parent regression.

Caption matching uses an optionally resolved `InternalGetWindowText` export to read stored captions without calling another thread's window procedure. Microsoft may change/remove that export; if unavailable, TSF title suppression is skipped with a diagnostic. The tray class is also a compatibility assumption, not a stable Microsoft API contract. Neither synthetic fixture establishes the exact native identity of the indicators in the user's photos.

During the latest Computer Use attempt, the agent restored the companion and clicked its observed Return to SafeBrowse control. The tool continued exposing only Default-desktop windows; refreshed selection and the supported activation retry ended with `failed to activate captured window`. The two photographed indicators could therefore not be inspected live after the change. The previously running session uses the older executable. Live visual removal of both indicators and physical Chinese IME composition remain unverified.

The subsequent ENG follow-up reproduced two additional, real Windows input windows in the authenticated hidden WebView2 fixture: `UAC_InputIndicatorOverlayWnd` and `UAC Input Indicator`. Both were naturally visible at `[1809,1089,1872,1152]`, a 63 × 63 rectangle at the bottom right. The earlier matcher omitted both classes. Production now matches these exact classes on its session desktop, in addition to the existing TSF/tray matches. These are Windows compatibility details, not documented API contracts; an independent [native enumeration example](https://scorpiosoftware.net/2021/07/03/processes-threads-and-windows/) also records these alternate-desktop classes.

`cargo run --locked --example worker_authorization_probe` verified actual suppression: after installing the production guard and pumping messages, the same two native window handles/classes changed from visible to hidden. Their child became nonvisible as well. The fixture does not artificially show or focus indicators, switch the input desktop, capture screen content, or inspect the live browsing session. It records only native class, rectangle and visibility metadata, and explicitly reports native hiding as unverified if no matching window was naturally visible. The five WebView2 processes still passed containment/termination checks.

The ENG follow-up passed `cargo test --locked --lib keyboard::language_bar -- --nocapture` (3 passed, 1 ignored subprocess entry invoked by its parent), `cargo fmt --check`, all-target Clippy with warnings denied, and the locked release build. The expanded native regression verifies initial visibility, late visibility, creation after guard installation, repeated shows, and restoration across threads and processes. Prefix/suffix class lookalikes, generic EN/ENG windows, candidate/picker windows, and equivalent indicators on another desktop stay visible. The full suite was not rerun for this focused change.

Computer Use again reached the live companion but continued returning only Default-desktop windows after its Return button was clicked; the companion then reported `window is minimized`. The real native fixture establishes the corrected Windows behavior on this machine, while visual confirmation in the user's current session and physical IME composition remain pending a restart into the updated executable.

The content-view builder now explicitly disables native WebView2 web messaging and host objects before navigation, including popup views. A native regression reads back both disabled settings from the actual production builder and confirms that bundled controls retain messaging and deliver their real UI commands. This closes unnecessary transport exposure left enabled by Wry when no application IPC handler was supplied.

Both website builders now share `browser::security::harden_content_view`, which additionally requires the WebView2 SmartScreen setting and reads it back before navigation. The production kiosk fixture verifies the setting is true. This checks the application contract, not whether Windows or enterprise policy overrides effective reputation checking; no live phishing or malware URL was visited.

The new launch tests cover complete/distinct internal handle arguments, worker/auth pairing, UUID desktop identities, refusal of an actually precreated desktop, bounded stalled/closed pipes, anonymous-pipe owner process IDs, inheritance flags, protocol version and length, PID/nonce binding, executable file identity, and job settings. A native fixture uses the production launcher and authentication path to start a real child, attempts a second process with replayed arguments, and confirms incomplete exchanges fail. Its idle child exits when the supervisor-owned job closes. Job membership is assigned atomically by `PROC_THREAD_ATTRIBUTE_JOB_LIST` at process creation before the suspended worker executes.

The same fixture authenticates a worker on that unused desktop, creates a hidden WebView with a fresh temporary user-data folder, executes compiled local HTML, and reads SmartScreen as required. All five runtime processes reported by the installed WebView2 environment were confirmed as members of that exact job; retained process handles then signaled exit after job closure. The fixture removed its temporary data successfully. This establishes compatibility and cleanup for the current runtime and a fresh profile, not every runtime version or a preexisting shared persistent-profile browser process.

Elevation tests cover mandatory-integrity boundaries, elevated/high-integrity refusal, and the live current process token. The main entry-point checks run before session locks, clipboard operations, desktop opening, profiles, or browser UI. No UAC elevation was requested. These native fixtures do not replace a complete interactive run of the new isolated application, desktop-restoration fault testing, or a test against same-user malware. They never clear the user's clipboard or switch the active desktop.

The reported immediate exit was reproduced with the release candidate: the current shell and Windows Explorer both had elevated tokens, and the read-only Windows configuration check found `EnableLUA = 0`. The production entry point refused startup with exit code 1 before opening a browsing session. On this machine, ordinary double-click launches therefore also require a standard Windows account or re-enabling UAC and restarting Windows. No Windows security setting was changed.

The startup-dialog follow-up passed `cargo test --locked --bin safebrowse` (5 tests), `cargo test --locked --lib security::integrity` (3 tests), formatting, all-target Clippy with warnings denied, and a locked release rebuild at the same `target/security-fix` path. The new tests cover standalone versus shared/failed console detection, suppression for worker/private/help arguments, and bounded Unicode-safe dialog text. This follow-up did not rerun the full suite.

Computer Use launched the rebuilt executable and visually verified the native **SafeBrowse couldn't start** dialog, including the administrator/UAC explanation and **OK** button. Clicking **OK** dismissed it; a refreshed window inventory confirmed no SafeBrowse window remained. A separate launch with redirected output returned exit code 1 and the same explanation without a dialog. The guard remains enforced, so a full interactive browsing session still needs a non-elevated Windows environment.

The user subsequently confirmed that normal startup works after restoring UAC, the ENG overlay is gone in the updated executable, and their screen recorder cannot capture the protected session but can capture a session started with the explicit recording flag. The recorder and capture mode were not specified. This supersedes the earlier pending user confirmation of startup and ENG removal; physical IME composition, recovery fault testing and broader Windows/capture-method coverage remain unverified.

The session-label follow-up removes the normal isolated-session badge from both the browser title bar and taskbar. Recording-enabled sessions retain their red recording indicators, and windowed protected sessions retain their Desktop mode labels. The full Rust suite, all 8 keyboard DOM tests, formatting and all-target Clippy with warnings denied passed after these template changes.

`cargo audit --json` against RustSec database commit `5a0ebedfe8bdd2e295b171f4162f8c977bcad9a5` (updated 2 September 2026) reported zero vulnerability entries. It reported maintenance warnings for GTK-related crates and `proc-macro-error`, plus the `glib` unsoundness advisory `RUSTSEC-2024-0429`. An offline, locked `cargo tree --target x86_64-pc-windows-msvc --edges normal,build` inspection confirmed that none of those warning packages belongs to this Windows build's normal or build dependency graph. The audit does not cover the separately installed WebView2 runtime or establish that SafeBrowse has no vulnerabilities.

Broader native capture exclusion and isolated desktop switching coverage still require the manual checks below. These are separate from verifying that bundled pages render.

The runtime-preflight follow-up passed three classifier tests, the five existing startup/authentication tests, and an explicit native discovery fixture (installed runtime `151.0.4129.107`). A child-only `WEBVIEW2_BROWSER_EXECUTABLE_FOLDER` override pointing to an empty disposable folder produced the real missing-runtime diagnostic and exit code 1. The same override left `--help` working and did not bypass worker authentication. No runtime was uninstalled and no user/machine environment setting was changed. Computer Use also verified the standalone native error dialog, its download address and OK button; dismissing it ended the process. Elevated-launch guidance now points to the README's documented UAC command and required Windows restart. The command was not executed.

The detachable keyboard's native fixtures verify that its initially hidden window is owned by the browser, belongs to the same UI thread/desktop, is not topmost, and requests exact capture exclusion unless the explicit recording flag is used. Tests move the same WebView controller between parents, preserve keyboard layout/shuffle/Caps state, and verify keyboard-only placement/drag/input IPC. Physical geometry tests include negative monitor origins and constrained displays. The full Rust suite, keyboard DOM tests, formatting, all-target Clippy and the locked release build passed. Automatic approval review initially blocked launching the recording-enabled fixture session with the generic reason "blocked by policy"; the user then launched it for Computer Use.

The subsequent live check used the rebuilt release executable in windowed recording mode and the loopback dummy page. Computer Use focused the draft field, entered `a` with the docked virtual keyboard, enabled Caps Lock, detached the keyboard, and visually confirmed the floating window retained uppercase keys. A single click on the browser's Minimize control minimized it; activation restored the browser and the draft, and the keyboard could be reopened and detached again. The tool exposed the floating keyboard as related screenshots and accessibility elements but did not list a separate targetable Window. Both drag and accessibility-key input were rejected because the hit-test resolved `msedgewebview2.exe "Chrome Legacy Window"` instead of the selected `safebrowse.exe "SafeBrowse"` window. Refresh/activation and the related screenshot did not resolve targeting. This is a tool limitation, not evidence of a keyboard failure. Live dragging, detached typing, typing after reattachment, detached minimize/restore behavior and mixed-DPI movement remain unverified; the corresponding native fixture results above are separate evidence. No application code was changed for this follow-up.

The user subsequently confirmed that the detachable keyboard works for them. This confirms the reported interaction on their machine; it does not establish mixed-DPI behavior or keylogger resistance.

The printing follow-up adds a native Windows chooser for the active website. Five focused kiosk tests passed, including a real bundled WebView2 DOM fixture checking one tab-bound request per click, Ctrl+P, repeat suppression, modifier rejection, internal/missing tabs and tab switching. Native policy tests reject other control surfaces and stale/closed/internal tab requests. All-target Clippy with warnings denied and the locked release build passed. The host calls `ShowPrintUI(SYSTEM)` only from the event loop, not from a WebView2 callback, and does not call a silent printing or PDF API. Successful return is not treated as proof of printing or cancellation.

On 4 September 2026, the user manually opened the release executable with `--windowed --allow-screen-recording` and the loopback `printing.html` fixture after automatic approval review had blocked the earlier launch. Computer Use verified four routes to the native **Microsoft Edge WebView2 - Print** dialog: the toolbar Print button, physical Ctrl+P from the focused website, the website's `window.print()` button, and Ctrl+P from the visually focused address bar with the detached keyboard open. Each dialog was dismissed with its actual **Cancel** button; no Print submission or PDF save was performed. The fourth route hid the floating keyboard, which reopened successfully afterward.

The dialog showed installed **Microsoft Print to PDF**, orientation and page controls, and **“This app doesn’t support print preview”**. Computer Use associated the dialog with `explorer.exe`. One Cancel attempt returned `coordinate input geometry is unavailable`; refreshing the screenshot-backed target and retrying succeeded. This was a tool-targeting limitation, not an observed application failure.

These live checks supersede the pending windowed dialog check. Actual printed/PDF output, print preview output, printer failures, private-desktop dialog placement and protected native-dialog capture remain unverified. Recording was explicitly allowed throughout this windowed test. No tests were rerun or application source changed for this live-check follow-up.

The download and printing-policy follow-up adds saved browser-wide download choices (Ask every time by default) and a default-off Printing switch below Downloads. Legacy permission files preserve their existing popup/site rules and acquire these safe defaults. Store tests verify persistence, invalid values, and failed replacement preserving committed memory and disk. The real bundled WebView fixture verifies Settings ordering/loading state and exact messages for all choices, plus download prompt filename injection resistance, long-address scrolling, visible buttons, default Cancel focus, and decisions tied to the displayed request ID. Host Print requests check the saved policy before changing keyboard focus or invoking the native dialog; the disabled error points to Settings. This setting does not intercept website `window.print()`.

The native download fixture uses loopback dummy text, a disposable browser profile, and a disposable output root; it never writes to the user's Downloads folder. It verifies no output directory before approval, denial, stale decisions, native writing of exact bytes on approval, distinct destinations, navigation/tab-attachment/final-broker-drop cancellation, and a maximum of four active transfers. A fifth approval is rejected before another output directory is created. Explicit native operation cancellation produces an interruption failure and releases the active slot without reporting completion. Truncated-response timeout/retry behavior was not tested to completion. Filename tests cover traversal, alternate streams, Windows device aliases, bidi/control characters, and bounded Unicode length; URL tests reject unsupported schemes and foreign-origin Blob URLs.

The full Rust suite (116 passed, 4 ignored), all eight keyboard DOM tests, and all-target Clippy passed for the policy integration. The earlier live printing check used the old build before this default-off policy. Computer Use closed that test session to release the executable for rebuilding. End-to-end interaction with the new download prompt and settings in the rebuilt app remains a separate manual check. No physical print job, PDF export, malicious download, SmartScreen detection, Mark-of-the-Web behavior, or protected-dialog capture was verified by these dummy fixtures. Completed files intentionally remain after session cleanup; incomplete-file cleanup is best effort.

A subsequent error-path review added fallback native transfer cancellation if WebView2 rejects the initial `SetCancel(true)`. Failure of both cancellation mechanisms now queues closure of the affected tab; a sole website tab is replaced by internal Settings, preventing automatic recreation. All six focused download tests passed after this fix, including injected initial/fallback failure classification and the native lifecycle fixture. The injected failures do not reproduce a broken runtime or prove that no data can be written before tab teardown when both native cancellation APIs fail.

After the final cancellation fix, `cargo fmt --check`, `cargo clippy --all-targets --locked -- -D warnings`, and `cargo build --release --locked` passed. The updated executable is `target/release/safebrowse.exe`; it includes the earlier startup, desktop, capture, keyboard and security changes. The closed test session was not automatically relaunched.

The subsequent website-printing follow-up installs `website_print_guard.js` in both website builders before navigation, with subframe injection explicitly requested. It replaces ordinary website `window.print()` calls with a dismissible in-page notice directing users to the native toolbar/shortcut. This applies independently of the host Printing setting; the host still invokes `ShowPrintUI` directly only when enabled. Website web messaging and host objects remain disabled.

Six Node behavior tests pass for blocked native calls, inert strict assignment, failed redefinition/deletion, repeated and early-call deduplication, preserved editable focus, dismissal, reentrant DOM callbacks and document teardown. `cargo run --locked --example print_wrapper_probe` additionally exercised the actual accessor guard on WebView2 `151.0.4129.107`, using hidden controls, a disposable profile and loopback HTML. Inline top-level code, ordinary and cross-origin frames, static/dynamic blank frames, `srcdoc` and `document.write` observed the locked wrapper in this run. Invoking only the verified wrapper twice produced exactly one guidance notice. The report is written to `target/website_print_guard_probe_report.json`.

The same probe found a concrete coverage gap: immediately after `window.open()` returns, the opener can observe and retain native `print` from both HTTP and blank popups. The popup's later property is wrapped, but that retained reference remains native. The probe never invoked native printing, so this is evidence of reference exposure rather than a demonstrated dialog bypass. Built-in PDF printing and other runtime versions were not tested. The guard is documented as best-effort compatibility suppression, never as a complete security boundary. The user's running protected session was left open.

The website-printing follow-up passed the full Rust suite (118 passed, 4 ignored), all 14 Node behavior tests, `cargo fmt --check`, and all-target Clippy with warnings denied. `cargo build --release --locked --target-dir target/website-printing` produced `target/website-printing/release/safebrowse.exe`; its `--help` command also passed without starting a session. This separate build includes all prior changes and leaves the in-use `target/release/safebrowse.exe` untouched. Close the current session before starting the new executable. No live browsing session was launched or interrupted for this follow-up.

## Manual test surface

1. Start a protected isolated session and confirm neither bar shows an Isolated desktop badge or an empty outline. Check a protected `--windowed` session retains its Desktop mode labels. Start a recording-enabled session separately: verify the red warning appears before a page opens and the red recording indicator remains after **OK**. Protection must remain the default on the next launch without the recording flag.
2. Serve `tests/fixtures` on loopback, navigate to `browser.html`, type a disposable draft, open/switch/close tabs and navigate Back/Forward. Check that the draft survives switching, the address updates after the same-page history button, and the messaging probe reports no SafeBrowse bridge. Test virtual keys in the address, password and bookmark fields using dummy data.
3. Close normally and confirm the temporary profile directory was removed. In a disposable Windows session, verify desktop return, re-entry through the companion, and worker-crash recovery. Never run clipboard-purge tests with clipboard content you need to keep.
4. In an isolated session, type with the virtual keyboard and immediately click minimize or Windows desktop once. Return through the companion, restore and move the browser, and confirm the taskbar stays at the monitor bottom. Open ENG, choose another installed layout, and type without clicking the input again; repeat after changing the layout with the physical keyboard. Check picker dismissal, mixed display scales, and Shift/Caps punctuation.
5. In Settings, test all three popup defaults and an exact-site exception. Verify Allow once, Always allow, Block, and Not now; navigate away with a request pending and confirm it cannot be approved later. Change/reset a saved permission and use Reload tabs to end access already active. Exercise hardware permissions only with disposable test content and explicit user consent.
6. Open the keyboard and focus a dummy input. Enable shuffled letters or Caps, detach, drag by the header, enter dummy text, and attach again. Verify state and input destination are preserved; dragging must not type a character. Close/reopen and minimize/restore the browser, check capture exclusion on the floating window, then repeat across monitors with different display scales.
7. Open `printing.html` on loopback. With Printing disabled (the default), verify the toolbar Print button and Ctrl+P explain that the controls can be enabled in Settings. Enable Printing directly below Downloads, restart and check the choice persists. Use Print and Ctrl+P from both the address bar and page, then cancel and confirm the browser remains usable. Repeat with the detached keyboard open and another active website. In both setting states, the website's own print button should show in-page guidance instead of a native dialog; repeat it and verify one dismissible notice. Script-triggered guidance must preserve a focused dummy field's caret. This check does not establish resistance to the popup reference exposure above. Settings and Bookmarks must not print through the toolbar. In a disposable session, test the same flows on the private desktop, including printer preferences and an installed Microsoft Print to PDF destination. Check two-page output, page ranges, offline/no-printer errors, and capture exposure of native dialogs separately from the browser. Any physical test job needs the tester's intentional confirmation.
8. Open `downloads.html` on loopback using dummy files only. With Ask every time (the default), verify both the ordinary link and generated Blob require confirmation, Cancel saves nothing, and Download saves the expected bytes beneath Downloads/SafeBrowse without opening them. Repeat the filename and check the first file stays unchanged. Test Disabled and Always allowed, restart to verify the saved choice, and change policy or navigate/close a tab with a request pending. Test a failed save and interrupted transfer; inspect partial-file remnants rather than assuming cleanup. Verify the app-owned prompt remains capture-protected in a normal session and usable at supported display scales. No downloaded fixture should be executed.

Example fixture server and native test launch, in separate terminals:

```powershell
python -m http.server 8766 --bind 127.0.0.1 --directory tests/fixtures
.\target\release\safebrowse.exe --windowed --allow-screen-recording --url http://127.0.0.1:8766/browser.html
```

These checks are not a security audit. See [SECURITY.md](../SECURITY.md) for boundaries and unsupported features.
